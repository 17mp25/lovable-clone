package com.mp.projects.lovable_clone.enums;

/**
 * Enum representing the current status of a user's subscription.
 * Used to track payment status and access privileges.
 */
public enum SubscriptionStatus {
    ACTIVE, TRIALING, CANCELED, PAST_DUE, INCOMPLETE
}
